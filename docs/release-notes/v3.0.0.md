# v3.0.0
<!-- release-title: TODO 一句话概括本次大版本 -->

## 版本概览

TODO：`v3.0.0` 是 `v2.3.9` 之后的大版本。这里写清楚本次的定位（做了什么、为谁做、和上一版的关系），一段话即可。

## 新增

TODO：本次新增的能力，一条一个要点，写清楚用户能看到什么变化。

## 优化

TODO：已有能力的改进。

## 修复

TODO：修复项。格式沿用历史版本：**一句话结论** + 现象 + 现在的行为 + 影响范围（哪些场景不受影响）。

## 破坏性变更

TODO：大版本如果有不向后兼容的改动（配置项重命名、接口字段调整、数据结构迁移等），必须在这里逐条列出并给出迁移办法。**没有则整节删除。**

## 升级说明

TODO：升级前需要注意什么、是否涉及数据迁移、历史任务与配置是否兼容、Docker 标签是否需要切换。

## 验证

- 后端回归测试：TODO
- 前端构建与类型检查（`npm run build` + vue-tsc）：TODO
- 最终发布前执行：`go test ./...`、`npm run build` 与 `scripts/release-preflight.ps1 -Version 3.0.0`。
